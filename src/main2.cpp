//shared list of tokens
//shared list of symbols

//Lexical analyzer shall read from a character string until NULL is encountered.
//At each understood token, the lexical analyzer shall create and add a token to the shared list.
//When a token is not understood, or invalid...?

//Preprocessor replaces tokens with their defined values (#define)
//Preprocessor ignores sections of code (#ifdef,#ifndef,#else/#endif)
//Proprocessor includes content from other files (#include)

